//default settings
defaultSettings = [];
defaultSettings['preset1']='1 Day';

//get a setting
//check if a setting exists in the database
//if it doesn't, get it from the defaults list.
function getSetting(setting){
	
	
	return value;
}